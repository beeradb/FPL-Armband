# Review: cutting the resident record to rules and pointers

**What was reviewed**: branch `cut-the-resident-record-to-pointers`, commits
`5ca003e..7d2fc00`: the tier-0 cut the research-store note `cut-claude-md-to-rules-and-
pointers` asks for — AGENTS.md went **84,868 → 42,668 bytes**. "Closed lines" and "What has
been measured" became named lists (verbatim titles + `→ **note**` pointers, with the
mitigation stated in the file and the do-not-delete warning); "Things that have already
bitten" keeps lesson titles + pinning test names; six pointer-less harness bullets moved
verbatim into "What the harness can resolve"; the season capability table moved to
docs/replay.md; the resident budget dropped 110 → 44 KB with the claim named.

**Reviewers that ran**:

- **fpl-findings-audit** — fourteen findings, all applied: ten where a surviving title read
  stronger than its source or a guard existed in no reachable surface (the clean-sheet
  retraction's "does NOT retract 1.5688" and leak-size guards; the fixture ladder's
  unmeasured-not-null hedge; the team-strength "mechanism, not measurement" twin; the xG
  harvest's "narrower than it reads" caveat; LeagueShrinkK's "unresolved, not a measured
  loss"; the OracleLineups data-state guard; the anchored-chip re-measurable-only warning;
  FPL_TEAM_FORM's open-and-unresolved status; the minutes floor's quote-nothing guard; the
  owed four-against-nothing comparison; the element_type GK-join trap; the four dropped
  pinning-test names; the sit-beside reporting rule). Two gained pointers repointed from
  transfer-policy to **xppilot**, where the evidence actually lives. The durable half was
  applied in the same commit pair: the moved bodies were appended verbatim into the seven
  pointed research-store notes, and constants-and-sweeps' stronger-than-verdict "it loses"
  phrasing was corrected.
- **fpl-docs-review** — four findings, all applied: the verbatim table move had imported a
  research-store arrow into user-facing docs (dropped, with the exception noted in the
  table's closing line); two sentences in docs/replay.md whose referents the move shifted
  (the archive-defects paragraph now points at the in-file table; the xPoints caveats
  sentence now says the verdict is named, title and pointer, under "Closed lines"); the
  "Where to go next" qualifier tightened.

**Skipped with the triage reason**: fpl-code-review — no code changed (one test-constant
change, documented). fpl-security-review — nothing on its surface.

**What was declined**: nothing. Every finding was applied.

**What could not be checked on this harness**: whether a pointer is *sufficient* — the tier-0
note's open risk; it will only be known from whether an idea gets rebuilt.

**The verdict recorded**: the resident file now holds rules and pointers; the verdicts live
in the research store, and every pointer was verified to resolve at the file level and, after
the audit, at the evidence level.
